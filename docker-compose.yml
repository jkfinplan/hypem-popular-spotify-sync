services:
  hypem-sync:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      SPOTIPY_CACHE_PATH: /app/data/.cache
    volumes:
      - ./data:/app/data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
